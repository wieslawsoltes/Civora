# Civora change log

## 0.10.0 — 23 September 2026

- Added bounded, insertion-aware revision comparison with verified original sizes/hashes, strict UTF-8/UTF-16 decoding, change navigation, full-result JSON evidence and same-scale SVG/raster wipe/overlay/side views.
- Added a controlled batch-rename planner, authoritative preview endpoint and one-transaction command with required reasons, source/version/ACL checks, final-name swaps, extension preservation and audit mapping.
- Added dark/light responsive workbench and rename dialogs, direct Versions/Document entry points, Shift+F2, phone unified comparison/options and sticky rename confirmation.
- Fixed delayed Blob/decoder and validation response races, account-context clearing, boolean metadata display and per-document visibility of aggregate rename audit entries.
- Added two independent bundles and an executable example. 62 new Node results and 24 new UI checks; retained suites remain. Current totals: 611 Node, 201 UI, 79 static HTTP, 29 libraries, eight examples.
- No native semantic CAD comparison, OS/CAD reference-path rewrite, signature, cloud/provider, HA, independent security or physical-device qualification is claimed.

## 0.9.0 — 23 September 2026

- Added permission-projected quick navigation over documents, folders, saved views and inert app commands; keyboard/phone access, bounded counts, explicit scope, delayed-response and session/revision guards.
- Added strict controlled-copy planner and real reducer preview; exact stored source selection, SHA-256 verification, per-batch content bounds, cross-project share checks, destination metadata/numbering and selected-ID reference remapping. One audit/revision, no source mutation or copied authority.
- Added responsive/touch copy editor, sticky confirmation controls, safe filename suggestions, shared metadata overrides and exact input-to-preview binding.
- Added active filter chips and unsaved-view status; filter reset preserves column/sort/density layout. Inspector previous/next retains its tab and batch selection.
- Fixed revision author field, footer/help release labels, switcher close/reopen race, stale Enter activation and focus restoration when one dialog opens another.
- Added two standalone libraries and one combined executable example; 74 new Node results and 23 new UI/transport checks. Existing qualification limits remain.


## 0.8.1 — intermittent old-interface repair

- Keep the compact Explorer shell on all 25 routes, including tools, settings, sign-in background and mobile navigation.
- One-time account/workspace-scoped appearance migration without changing shared views or document data.
- Atomically bundle deployed HTML/CSS/JS, derive build IDs, rebuild on startup and scope verified offline caches by install/build.
- Add visible version/update diagnostics; no automatic reload or site-data clearing.
- Add 26 Node and 11 UI regression checks; totals 475 Node, 154 UI and 65 static HTTP.
- Preserve original server, permissions, local-system, mobile and touch workflows. Browser-origin/physical-device qualification remains outstanding.

## 0.8.0 — compact Explorer visual design

- Added standard desktop toolbars, familiar menu groups, explicit datasource hierarchy, yellow folders, blue/slate themes and a single-line detailed register.
- Added Explorer/Ribbon/Review presets and independent local appearance settings without rewriting shared views or workspace data.
- Added current-source folder properties and typed metadata attributes beside retained actual-byte preview and controlled-record tabs.
- Added keyboard menubar/tree/tab/F6 navigation, preserved focus and fixed selected-project collapse and selection-induced double-click shifts.
- Retained all 25 tools, phone cards, touch action menus, hybrid touch sizing and drawing/model gestures.
- Added four pure exports to the existing Explorer library; 10 Node and 20 UI regression checks. Current totals: 449 Node, 143 UI, 25 bundles.
- Updated dark-mode expectations and mobile selectors to the actual intended 0.8 interface; normal browser-origin/device qualification remains outstanding.

## 0.7.0 — controlled sets and touch-first Explorer

- Added ordered live/fixed document sets, exact-definition versions, reasoned manager locks, current-access projections, verified ZIPs and revision-pinned delivery drafts.
- Added authenticated resolve/export endpoints, bounded concurrent archive work, cancellation, integrity/revocation checks and restart coverage.
- Retained the original 1,000-member legacy-set capacity; no automatic pinning or access grants.
- Added phone cards, five-action navigation, searchable all-tools sheets, touch menus/selection, photo file input and explicit layout preferences.
- Added image pan/pinch, two-tap boxes, phone annotation panels, model gestures and Elements/properties panels; fixed model Fit for narrow aspect ratios.
- Added three independent libraries and an executable example; 439 Node tests, 123 UI checks, updated matrices, upgrade/security notes and artifact verification.
- Full native CAD/BIM, ProjectWise API compatibility, real-device/browser-origin and independent security/accessibility qualification remain outstanding.

## 0.6.0 — modern engineering Explorer

- Rebuilt Documents as a familiar, original tree/list/address/ribbon/context/preview workspace; all existing app tools retained.
- Added metadata columns, drag/order/resize, natural/multi-sort, group counts, advanced/inline/quick filters, saved personal/shared views and bounded paging.
- Added scoped reference-only pins, recent IDs, navigation history, stable links and post-sign-in address continuation.
- Added permission-projected query API and authoritative move preview; atomic same-project bulk moves, cut/paste and drag/drop with unchanged file bytes.
- Preserved current-read-checked notifications for source/destination folder moves; rejected stale asynchronous preview/form/identity responses.
- Added independent Explorer library/example, domain/real HTTP and actual-UI/explicit-transport tests, feature/UI matrices and upgrade guidance.
- Native CAD, external-provider, real browser-origin and enterprise/independent security qualification remain outstanding.


## 0.5.0 — governed workflow automation

- Added a pure declarative rule engine: schema-typed AND/OR conditions, reason guards, bounded assignments, stable rule ordering and current-document transactional gates. No eval, user scripts, authorization bypass or automatic approval.
- Added read-only server/local transition previews, exact-input UI invalidation and explicit preview-revision checks on real transition commands.
- Added project deadline policies for reminders, overdue notices and guarded single-pending-voter delegation. Preserve decisions and quorum; recheck policy owner, pinned context, permissions and optional separation of duties.
- Added a single-node server scheduler, bounded 200-event scans, CAS conflict recomputation/coalescing, durable deduplication/outcome receipts, idle no-op behavior and graceful shutdown. Real restart tests run unattended, not through simulated browser timers.
- Added reference-only personal notifications, scoped event watches, live ACL projection, pagination/read/snooze endpoints, per-member bounded history and actual-resource deep links. No external notification transport.
- Added Workflow automation and Notifications workspaces (25 screens total), rule/policy/preview dialogs, due queue/history export, actual server scheduler health, light/dark/mobile layouts and dedicated icons.
- Added the 21st standalone module, reusable automation example, feature/upgrade/security documentation and strict imported automation-record validation.
- 61 additional Node results (49 pure/domain/runner tests and one authenticated HTTP suite with 11 nested checks): 306 passing results in total. Added 14 actual-memory application UI checks and 5 explicitly substituted transport checks; prior UI suites retained.
- No native CAD fidelity, horizontal scalability, live-provider qualification or independent security assessment is claimed by these changes.

## 0.4.0 — controlled delivery

- Typed metadata, defaults, bounded constraints and explicit exception reporting.
- Transactional project numbering with safe monotonic sequences and manual-number policy.
- Atomic bulk metadata updates and protected editable CSV round trip with preview/conflict handling.
- Ordered review templates, per-stage quorum/decisions/deadlines, optional separation of duties, reasoned reassignment and version+metadata publication gates.
- Immutable baselines, dependency closure, exact record comparisons, verified original-byte ZIPs and baseline-backed transmittals.
- Textual DXF/SVG/OBJ reference discovery with user-confirmed mappings and source-version checks.
- Document control, Revision baselines and My work screens; rich metadata upload controls and selection/bulk improvements.
- Fixed select-all rerender/indeterminate state and expanding file badges in choice rows.
- 65 additional Node results (51 pure/domain cases and 14 real HTTP/SQLite results); 245 total passing results, plus 16 document-control UI checks.
- No native-engine, enterprise-scale, live-provider or independent-security parity claim.

## 0.3.0 — Local system support

- Added opt-in authenticated loopback companion and Windows/macOS/Linux launch scripts.
- Added scoped roots, explicit user grants, read-only mode, same-host root locks and guarded native launching.
- Added direct browser directory adapters/bookmarks and immutable folder workspace repository, checksum verification and explicit recovery.
- Added checkout-to-disk working copies, exact hash/revision check-in, conflict preservation and per-account indexes.
- Added local explorer, safe file operations/recovery, text encoding preservation, hierarchy transfers, manifest exports and full directory-drop reader traversal.
- Added five independent modules (sixteen total), a local UI workspace (twenty pages total) and documentation/examples.
- Preserved native check-in/import and folder-repository MIME metadata without altering original bytes.
- Added 45 Node test results; full runner: 180 passing. Added 15 isolated local UI checks. Real browser-origin, mounted-drive, native Windows/macOS and independent security qualification remain outstanding.

# Changelog

## 0.2.0 — 22 September 2026

Hierarchical server-enforced access control and safe metadata projections replace implicit member-wide document visibility. Added user/group policies, effective permission inspection, scoped original/rendition content and live invalidation, plus a real same-origin team sign-in gate.

Added independent recipient enrollment/session service with revision-frozen downloads, receipts, expiry and revocation; durable rendition operations, bounded workers and external converter protocol; geometry import subsets, pinned model federation and bounded clash analysis; session-bound append-only cloud snapshot synchronization with explicit conflicts; operational metrics and chained security evidence.

Redesigned navigation and workspaces, retaining original workflows. Added original structural/services/DXF demo sources, eleven standalone library bundles and standalone portal UI. Added migration/deployment/security-review documentation and regression coverage. Fixed the standalone replacement escaping bug, permission-edge cases, sync pause/cycle handling, recipient enrollment races and rendition-worker early-shutdown hang.

135 Node tests passed, plus 19 isolated app UI checks and 4 recipient component fixture checks. See TESTING.md for actual evidence and all unqualified paths. Complete native CAD fidelity, enterprise-scale operation and independent security qualification are not delivered.

## 0.1.0

Initial local-first document/project/review workspace, original-byte version control, basic previews, backup adapters and optional authenticated SQLite team service.
